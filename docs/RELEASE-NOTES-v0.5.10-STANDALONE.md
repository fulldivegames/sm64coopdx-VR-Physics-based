# SM64 Co-Op DX VR Quest Standalone v0.5.10

This standalone hotfix adds an accessible DynOS pack directory:

```text
/sdcard/SM64VR/dynos/packs/
```

The application creates the directory automatically after storage access is granted. Extract DynOS packs into it, restart the game, and enable them from the DynOS menu.
